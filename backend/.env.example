PORT=4000
NODE_ENV=development

# Optional Supabase config (falls back to SQLite in-memory if empty)
SUPABASE_URL=
SUPABASE_KEY=

# Security Configs
# Daily creations limit per IP/device (default is 3, abuse is triggered if > 5)
DAILY_ROOM_LIMIT=3
ABUSE_ROOM_LIMIT=5
VPN_CHECK_ENABLED=true
